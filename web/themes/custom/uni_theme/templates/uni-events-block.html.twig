<div class="events-block">
  <h3>{{ 'Upcoming Events'|t }}</h3>
  
  {% if events %}
    {% for event in events %}
      <div class="event-item">
        <div class="event-date">
          <span class="day">{{ event.day }}</span>
          <span class="month">{{ event.month }}</span>
        </div>
        <div class="event-details">
          <h4>{{ event.title }}</h4>
          <p>📍 {{ event.location }}</p>
        </div>
      </div>
    {% endfor %}
  {% else %}
    <p>{{ 'No upcoming events.'|t }}</p>
  {% endif %}
</div>